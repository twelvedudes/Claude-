# Copy to .env and edit. Used by docker-compose.yml.
# These set both the MariaDB account and what the server connects with.

SQL_DATABASE=xidb
SQL_USER=xiadmin
# CHANGE THIS before exposing the server anywhere.
SQL_PASSWORD=change_me_please
